import { GroupsToStringPipe } from './groups-to-string.pipe';

describe('GroupsToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupsToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
